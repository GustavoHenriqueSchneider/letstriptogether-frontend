
  # High-Fidelity Prototype Design testes

  This is a code bundle for High-Fidelity Prototype Design testes. The original project is available at https://www.figma.com/design/NSKND5SOwVaBCJJOao4h6S/High-Fidelity-Prototype-Design-testes.

  ## Running the code

  Run `npm i` to install the dependencies.

  Run `npm run dev` to start the development server.
  